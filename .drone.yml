pipeline:

  build:
    privileged: true
    image: quay.io/ukhomeofficedigital/openjdk8:v1.1.0
    environment:
      - DOCKER_HOST=tcp://127.0.0.1:2375
    commands:
      - export ARTIFACTORY_USER=pttg-fs-robot
      - export ARTIFACTORY_PASS=${DOCKER_ARTIFACTORY_PASSWORD}
      - export PTTG_GH_TOKEN=${PTTG_GH_TOKEN}
      - ./gradlew build
    when:
      event: [push, pull_request, tag]

  docker-build:
    privileged: true
    image: docker:1.11
    environment:
      - DOCKER_HOST=tcp://127.0.0.1:2375
    commands:
      - docker build -t pttg-fs-api .
    when:
      event: push
      branch: master

  tag-latest-docker-image:
    image: docker:1.11
    environment:
      - DOCKER_HOST=tcp://127.0.0.1:2375
    commands:
      - docker login -u="ukhomeofficedigital+pttg" -p=${DOCKER_PASSWORD} quay.io
      - docker tag pttg-fs-api quay.io/ukhomeofficedigital/pttg-fs-api:latest
      - docker tag pttg-fs-api quay.io/ukhomeofficedigital/pttg-fs-api:build-${DRONE_BUILD_NUMBER}
      - docker push quay.io/ukhomeofficedigital/pttg-fs-api:latest
      - docker push quay.io/ukhomeofficedigital/pttg-fs-api:build-${DRONE_BUILD_NUMBER}
    when:
      event: push
      branch: master

  predeploy:
    image: plugins/git
    environment:
      - DOCKER_HOST=tcp://127.0.0.1:2375
    commands:
      - echo 'In predeploy, about to clone kube project'
      - git clone https://github.com/UKHomeOffice/kube-pttg-fs-api.git
      - echo 'In predeploy, after clone kube project'
    when:
      event: [push, deployment]
      branch: master

  deploy-to-dev-from-build-number:
    image: quay.io/ukhomeofficedigital/kd:v0.2.2
    environment:
      - DOCKER_HOST=tcp://127.0.0.1:2375
      - KUBE_NAMESPACE=pt-i-dev
      - ENVIRONMENT=dev
      - VERSION=build-${DRONE_BUILD_NUMBER}
      - KUBE_SERVER_DEV=https://kube-dev.dsp.notprod.homeoffice.gov.uk
    commands:
      - echo 'In deploy-to-dev-from-build-number:'
      - echo 'deploy build $VERSION'
      - cd kube-pttg-fs-api
      - ./deploy.sh
    when:
      event: push
      branch: master

  deploy_to_dev:
    image: quay.io/ukhomeofficedigital/kd:v0.2.2
    environment:
      - DOCKER_HOST=tcp://127.0.0.1:2375
      - KUBE_NAMESPACE=pt-i-dev
      - ENVIRONMENT=dev
      - VERSION=build-${DRONE_BUILD_NUMBER}
      - KUBE_SERVER_DEV=https://kube-dev.dsp.notprod.homeoffice.gov.uk
    commands:
      - cd kube-pttg-fs-api
      - ./deploy.sh
    when:
      event: push
      branch: master

  deploy_to_test:
    image: quay.io/ukhomeofficedigital/kd:v0.2.3
    environment:
      - DOCKER_HOST=tcp://127.0.0.1:2375
      - KUBE_NAMESPACE=pt-i-test
      - ENVIRONMENT=test
      - VERSION=build-${DRONE_BUILD_NUMBER}
      - KUBE_SERVER_DEV=https://kube-dev.dsp.notprod.homeoffice.gov.uk
    commands:
      - cd kube-pttg-fs-api
      - ./deploy.sh
    when:
      event: deployment
      environment: test

  deploy_to_preprod:
    image: quay.io/ukhomeofficedigital/kd:v0.2.3
    environment:
      - DOCKER_HOST=tcp://127.0.0.1:2375
      - KUBE_NAMESPACE=pt-i-preprod
      - ENVIRONMENT=preprod
      - VERSION=build-${DRONE_BUILD_NUMBER}
      - KUBE_SERVER_DEV=https://kube-dev.dsp.notprod.homeoffice.gov.uk
    commands:
      - cd kube-pttg-fs-api
      - ./deploy.sh
    when:
      event: deployment
      environment: preprod

  deploy_to_prod:
    image: quay.io/ukhomeofficedigital/kd:v0.2.3
    environment:
      - DOCKER_HOST=tcp://127.0.0.1:2375
      - KUBE_NAMESPACE=pt-i-prod
      - ENVIRONMENT=prod
      - VERSION=build-${DRONE_BUILD_NUMBER}
      - KUBE_SERVER_PROD=https://kube.dsp.digital.homeoffice.gov.uk
    commands:
      - cd kube-pttg-fs-api
      - ./deploy-prod.sh
    when:
      event: deployment
      environment: prod

services:
  dind:
    image: docker:1.11-dind
    privileged: true
    command:
      - "-s"
      - "overlay"

